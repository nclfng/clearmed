# ── Orchestration (model-agnostic) ─────────────────────────────────────
langgraph             # state-graph orchestration: deterministic DAG + bounded loops
langchain-core        # message / prompt primitives used by langgraph
litellm               # one interface to every model — we point it ONLY at local Ollama models (free, no API key/billing)

# ── Local models (FREE — no API keys, no paid trial credits, ever) ─────
# Install Ollama separately (not a pip package): https://ollama.com/download
# Then `ollama pull gemma2:2b` (or another small model) and leave it running locally.

# ── RAG / retrieval ────────────────────────────────────────────────────
chromadb              # default embedding function is a bundled LOCAL model — free, no API key
sentence-transformers  # use this (not a hosted embeddings API) if you want a different embedding model
# faiss-cpu               # optional offline vector index

# ── Evaluation ─────────────────────────────────────────────────────────
textstat              # Flesch-Kincaid, SMOG readability
scikit-learn          # Cohen's kappa, agreement metrics

# ── Notebooks (all EDA / calibration / ablation / analysis work) ───────
jupyterlab
nbstripout            # git filter: strip notebook output on commit so diffs stay reviewable

# ── Data / analysis ────────────────────────────────────────────────────
pandas
numpy
datasets
openpyxl              # read the MedAESQA methods .xlsx
matplotlib
seaborn

# ── Demo (December: HF Spaces or Cloud Run) ────────────────────────────
gradio
# streamlit            # alternative demo framework

# ── Optional alternatives ──────────────────────────────────────────────
# crewai               # alternative orchestration framework
